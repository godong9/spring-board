server:
  port: 8080
  context-path: /

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
    open-in-view: false

---

spring:
  profiles: local
  datasource:
    username: test
    password: test
    url: jdbc:mysql://localhost/board?useUnicode=true&characterEncoding=utf8&useSSL=false

